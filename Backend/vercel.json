{
  "version": 2,
  "builds": [
    {
      "src": "*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/users/(.*)", "dest": "./routes/userRoutes" },
    { "src": "/notes/(.*)", "dest": "./routes/noteRoutes" },
    { "src": "/(.*)", "dest": "/backend/index.js" }
  ]
}
